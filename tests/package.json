{
  "scripts": {
    "pretest": "yarn build && ./build-near-vm-runner-standalone.sh",
    "test": "yarn asp:ci",
    "asp:ci": "asp",
    "asp": "yarn asp:ci --verbose",
    "build": "node asconfig.js"
  }
}
