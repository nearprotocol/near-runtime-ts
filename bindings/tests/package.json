{
  "name": "near-bindgen",
  "version": "0.0.0",
  "description": "Tests for near bindings",
  "main": "test.js",
  "scripts": {
    "build:as": "near-asc assembly/test.ts --validate --runtime stub --binaryFile out/test.wasm --textFile out/test.wat --runPasses dce --debug",
    "build": "npm run build:as",
    "test": "npm run build && node --experimental-wasm-bigint ./test.js"
  },
  "dependencies": {
    "near-runtime-ts": "file:../.."
  }
}
